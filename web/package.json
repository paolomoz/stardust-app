{
  "name": "stardust-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "stardust web app — conversational-first redesign studio (frontend)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}
